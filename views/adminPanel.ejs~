<%include header.ejs %>
<style>
table, th, td {
    border: 1px solid black;
    border-collapse: collapse;
    background-color: #f1f1c1;
}
th, td {
    padding: 15px;
     color : #701818
}
th {
    padding: 15px;
    font-size: 200%;
    color : #4b346e;
}
h1 {
    font-family:verdana;
   color : #6c2a62;
   font-size:300%;
}
</style>
<div class="main">
    <section class="module bg-dark-30 about-page-header" data-background="img/portfolio/landing.jpg">
        <div class="container">
            <div class="row">
                <div class="col-sm-6 col-sm-offset-3">
                    <h1 class="module-title font-alt mb-0">ADMIN PANEL</h1>
                </div>
            </div>
        </div>
    </section>
    <div class="text-center">
                                      <div class="btn-group mt-50">
                                      <a class="btn btn-border-d btn-circle" href="/adminposting"><i class="fa fa-java"></i>Write Government Decisions/Posts</a>
                                </div>
     <section class="module">
        <div class="container">
        <div class = "row">
         <div class="col-sm-8 col-sm-offset-2">
<h1 align="center"><b>USERS LIST </b></h1><br>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<table style = "width:100%">
<tr>
<th > <b>Status </b></th>
<th > <b>User-Email </b></th>
</tr>
<% for(var i=0;i<users.length;i++){ %>
<tr>
	<% if(users[i].isBanned == true){%>
	<td>	<input type = "checkbox" checked="true" onclick="unbanUser('<%= users[i]._id %>')" ></td><td><%= users[i].email %></td>
	<% }else{ %>
		<td><input type = "checkbox" onclick="banUser('<%= users[i]._id %>')"></td><td><%= users[i].email%><br></td>
	<% } %>
<% } %>
</tr>
</table>
<p style="color:red">** Note: If the box is checked that means the user in banned and not checked means user is not banned</p>
<h1 align="center"><b>List  to select ADMIN:- </b></h1><br>

<table style = "width:100%">
<tr>
<th > <b>Status </b></th>
<th > <b>User-Email </b></th>
</tr>
<% for(var i=0;i<users.length;i++){ %>
<tr>
	<% if(users[i].group== 1){%>
	<td>	<input type = "checkbox" checked="true" onclick="removeadmin('<%= users[i]._id %>')" ></td><td><%= users[i].email %></td>
	<% }else{ %>
		<td><input type = "checkbox" onclick="makeadmin('<%= users[i]._id %>')"></td><td><%= users[i].email%><br></td>
	<% } %>
<% } %>
</tr>
</table>
<p style="color:red">** Note: If the box is checked that means the user in banned and not checked means user is not banned</p>
<script type="text/javascript">
	function banUser(userId){
		var parameters = {}
		parameters.userId = userId
		$.get('/banuser',parameters,function(data){

		})
	}
	function unbanUser(userId){
		var parameters = {}
		parameters.userId = userId
		$.get('/unbanuser',parameters,function(data){

		})
	}

	function removeadmin(userId){
		var parameters = {}
		parameters.userId = userId
		$.get('/removeadmin',parameters,function(data){

		})
	}

	function makeadmin(userId){
		var parameters = {}
		parameters.userId = userId
		$.get('/makeadmin',parameters,function(data){

		})
	}


</script>
</div>
 </div>
 </div>
    </section>
<%include footer.ejs%>
